import styled from 'styled-components';
import { lineHeight, fontSize, color, fontWeight } from 'styled-system';

const StyledDynamicComponent = styled.p`
  ${lineHeight}
  ${fontSize}
  ${color}
  ${fontWeight}
`;

export { StyledDynamicComponent };
