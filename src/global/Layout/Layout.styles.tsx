import styled from 'styled-components';

const MainLayout = styled.main`
  display: grid;
  grid-gap: 4rem;
  grid-template-columns: 3fr 1fr;
  max-width: 90%;
  margin: 1rem auto;
`;

export const Styled = {
  MainLayout,
};
