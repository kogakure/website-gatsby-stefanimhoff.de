import styled from 'styled-components';

const MainLayout = styled.main`
  background: var(--colorBackground);
  margin: 1rem auto;
  padding-left: 2rem;
  padding-right: 2rem;
`;

export const Styled = {
  MainLayout,
};
