import styled from 'styled-components';

const Icon = styled.span`
  display: inline-flex;
  fill: var(--colorForeground);
  margin-left: 0.2em;
  position: relative;
  top: 0.2em;
`;

export const Styled = {
  Icon,
};
