import styled from 'styled-components';
import {
  border,
  padding,
  paddingBottom,
  paddingLeft,
  paddingRight,
  paddingTop,
} from 'styled-system';

const Spacer = styled.div`
  ${border}
  ${paddingBottom}
  ${paddingLeft}
  ${paddingRight}
  ${paddingTop}
  ${padding}
`;

export const Styled = {
  Spacer,
};
