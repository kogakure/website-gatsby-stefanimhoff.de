import styled from 'styled-components';

const LocalizedDateContainer = styled.time`
  cursor: pointer;
`;

const Kanji = styled.span``;

export const Styled = {
  LocalizedDateContainer,
  Kanji,
};
