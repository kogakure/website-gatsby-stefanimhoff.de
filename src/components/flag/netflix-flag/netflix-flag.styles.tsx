import styled from 'styled-components';

import { Flag } from '..';

const NetflixFlag = styled(Flag)`
  background-color: rgb(207, 0, 0);
  border-color: rgb(207, 0, 0);
  color: white;
`;

export const Styled = {
  NetflixFlag,
};
