import React from 'react';
import { Meta } from '@storybook/react/types-6-0';

import { Circle } from '.';

export default {
  title: 'Icons/Circle',
} as Meta;

export const Icon = () => <Circle />;
