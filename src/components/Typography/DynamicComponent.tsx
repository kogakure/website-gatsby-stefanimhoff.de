import styled from 'styled-components';
import {
  border,
  borderColor,
  borderRadius,
  borderStyle,
  borderWidth,
  color,
  display,
  fontFamily,
  fontSize,
  fontStyle,
  fontWeight,
  letterSpacing,
  lineHeight,
  size,
  space,
  textAlign,
  textStyle,
} from 'styled-system';

export default styled.div`
  ${borderColor}
  ${borderRadius}
  ${borderStyle}
  ${borderWidth}
  ${border}
  ${color}
  ${display}
  ${fontFamily}
  ${fontSize}
  ${fontStyle}
  ${fontWeight}
  ${letterSpacing}
  ${lineHeight}
  ${size}
  ${space}
  ${textAlign}
  ${textStyle}
`;
