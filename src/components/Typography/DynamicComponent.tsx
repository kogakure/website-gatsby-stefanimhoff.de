import styled from 'styled-components';
import {
  borderRadius,
  color,
  fontFamily,
  fontSize,
  fontStyle,
  fontWeight,
  letterSpacing,
  lineHeight,
  size,
  space,
  textAlign,
  textStyle,
} from 'styled-system';

export default styled.div`
  ${borderRadius}
  ${color}
  ${fontFamily}
  ${fontSize}
  ${fontStyle}
  ${fontWeight}
  ${letterSpacing}
  ${lineHeight}
  ${size}
  ${space}
  ${textAlign}
  ${textStyle}
`;
