import React from 'react';

import { Typography } from '.';

const { Subline } = Typography;

export default {
  title: 'Typography/Subline',
};

export const Default = () => <Subline>Subline</Subline>;
